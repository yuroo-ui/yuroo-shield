fix(web): isolate render errors so they don't trip the network catch

Wrap showVerdict() in its own try/catch — previously a render-time error
would bubble to the outer fetch catch and trigger showError("Network error"),
masking the real cause. Now render errors log to console + show their actual
message, while the verdict panel stays hidden cleanly.
